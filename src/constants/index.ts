export const FRAME_PER_SECONDS = 1000 / 30;

export const DISPLAY_PADDING = 32;

export const MAX_VELOCITY = 35;

export const DEFAULT_MOB_JUMP_POWER = 20;

export const DEFAULT_MOB_SPEED = 0.55;

export const DEFAULT_FRICTION = 0.85;

export const DEFAULT_GRAVITY = 2;

export const CROUCHING_FRICTION_COEFFICIENT = 0.08;

export const CANVAS_CONTEXT = '2d';
